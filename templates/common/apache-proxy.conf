<VirtualHost 127.0.0.1:80>
    ServerName {{project.domain}}
    ProxyPreserveHost On
    <Proxy *>
        Order allow,deny
        Allow from all
    </Proxy>
    ProxyPass / http://localhost:{{docker.port}}/
    ProxyPassReverse / http://localhost:{{docker.port}}/
</VirtualHost>
