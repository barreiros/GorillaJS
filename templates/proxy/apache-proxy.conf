<VirtualHost *:*>

    ServerName {{project.domain}}

    DocumentRoot /var/www/html

    ErrorDocument 502 /error/index.html
    ErrorDocument 503 /error/index.html

    ProxyPreserveHost On
    ProxyPass /error/ !
    ProxyPass / http://{{project.slug}}/
    ProxyPassReverse / http://{{project.slug}}/


</VirtualHost>
