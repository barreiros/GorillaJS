vcl 4.0;

sub vcl_recv {

   if (req.http.host == "{{project.domain}}" || req.http.host == "www.{{project.domain}}") {

        return (pass);

        # You should filter all subroutines by the domain name to avoid global configurations.
        
   }

}
