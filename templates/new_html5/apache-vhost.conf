<VirtualHost *>

    Servername {{project.domain}}
    ServerAlias www.{{project.domain}}

    DocumentRoot /var/www/{{project.domain}}/application

    ErrorLog /var/log/apache2/{{project.domain}}-error.log
    CustomLog /var/log/apache2/{{project.domain}}-access.log combined
  
    <Directory /var/www/{{project.domain}}/application>
        Require all granted
        Allowoverride ALL
    </Directory>

</VirtualHost>
