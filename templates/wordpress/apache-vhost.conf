<VirtualHost *:80>

  Servername ${apache.vhosturl}

  DocumentRoot /var/www/${apache.vhosturl}/${project.serverfolder}

  Options -Indexes

  ErrorLog /var/log/apache2/${apache.vhosturl}-error.log
  CustomLog /var/log/apache2/${apache.vhosturl}-access.log combined
  
</VirtualHost>
