web:
  image: gorillajs/apache-base
  ports:  
    - "${docker.port}:80"
  volumes:
    - ../${docker.gorillafolder}:/root/templates
    - ../${project.serverfolder}:/var/www/${project.domain}/${project.serverfolder}
  links:
    - mysql:database
mysql:
  image: gorillajs/mysql
  expose: 
    - 3306
  volumes:
    - ../${docker.gorillafolder}:/root/templates
    - ../${project.datafolder}:/var/lib/mysql

